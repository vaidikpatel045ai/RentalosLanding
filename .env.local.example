# Copy this file to .env.local and set a real value. .env.local is
# git-ignored by default (see .gitignore), so your real password never
# gets committed.

# Shared password for the internal /admin/inquiries page (demo request
# list). Anyone with this password can view submitted leads.
ADMIN_PASSWORD=
